export enum AxisNames {
	x = 'x',
	y = 'y',
	z = 'z',
}

export const fontSize = {
	axis: '15px',
	legend: '16px',
	label: '17px',
	title: '21px',
};

export const availableAxes = Object.values(AxisNames);
